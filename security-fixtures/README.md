# Security scanner fixture

This directory is intentionally vulnerable and exists only to verify Snyk and
Dependabot reporting in a temporary pull request.

Nothing in this directory is imported, installed, packaged, or deployed by the
Linker application. Delete the entire directory and its Dependabot entry after
the scanner demonstration.

